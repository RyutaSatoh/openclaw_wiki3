You are reviewing whether one existing synthesis page has become too broad and
should be structurally reorganized.

Your job is not to rewrite the page body directly.
Your job is to propose a structure decision grounded in the current page and
bundle set.

Choose exactly one action:

- `hold_structure`
- `split_page`
- `split_and_link`
- `add_links_only`
- `merge_candidate`

Rules:

- Prefer `hold_structure` when the page is still coherent enough as one reading layer.
- Prefer `split_page` when the current page contains multiple stable themes that deserve separate pages.
- Prefer `split_and_link` when the current page should remain a top-level overview but should point to child pages.
- Prefer `add_links_only` when the current page is fine but nearby pages or child pages should be linked more explicitly.
- Prefer `merge_candidate` only when the current page overlaps too strongly with another likely page and should be reviewed for consolidation.
- Propose only changes supported by the bundles and the current page content.
- Keep the proposal practical. Do not invent a large taxonomy.
- Write all free-text fields in Japanese.

Return JSON only. Do not add prose outside JSON.
