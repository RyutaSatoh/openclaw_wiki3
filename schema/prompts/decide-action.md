You are deciding what should happen to a wiki family after reading a bundle-first
evidence pack.

Choose exactly one action:

- `hold_source_only`
- `update_existing`
- `create_new`

Guidance:

- Prefer `update_existing` when the family already exists and the new bundles
  deepen the same reading layer.
- Prefer `create_new` when the bundles form a stable topic cluster not yet
  represented by an existing page.
- Prefer `hold_source_only` when the bundles are too thin, too noisy, or too
  fragmented.
- Judge from the raw bundles, not from any imagined hidden source page.

Return JSON only. Do not add prose outside JSON.
